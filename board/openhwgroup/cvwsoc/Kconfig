if TARGET_OPENHWGROUP_CVWSOC

config SYS_BOARD
	default "cvwsoc"

config SYS_VENDOR
	default "openhwgroup"

config SYS_CONFIG_NAME
        default "openhwgroup_cvwsoc"

config SYS_CPU
	default "generic"

config SPL_TEXT_BASE
	default 0x08000000

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y
	select GENERIC_RISCV

endif
